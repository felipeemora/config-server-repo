springdoc:
    api-docs:
        path: /swagger-ui.html
        enabled: false

server:
    port: 48154

spring:
    jpa:
        show-sql: true
    datasource:
        url: jdbc:h2:mem:customer
        username: jmora
        password: 1234
        driver-class-name: org.h2.Driver
    h2:
        console:
            enabled: true

custom:
    activeprofileName: prod